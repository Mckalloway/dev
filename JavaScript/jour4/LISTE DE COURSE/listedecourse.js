/*var courses = ["cafe", "lait", "chocolat", "sucre"];

console.log(courses);

var item = window.prompt("ajoutez un article à votre liste de course") ;

//ajout

function ajouter(course) {
    courses.push(course);
}
ajouter(item);

console.log(courses);



//supp

var sup = window.prompt("supprimez un article de votre liste");

function supprimer(course) {
    var index = courses.indexOf(course);
    if (index != -1) {
        courses.splice(index, 1);
    }
    
}
supprimer(sup);

console.log(courses);

//toutsupp


var toutSup =window.prompt("voulez-vous réinitialiser la liste ?");



console.log(courses);
*/




/*
//AJOUTER

function ajouter() {
    courses.push(window.prompt("Ajoutez un article à la liste de course"));
}

ajouter();

console.log(courses);

ajouter();

console.log(courses);

ajouter();

console.log(courses);

ajouter();

console.log(courses);



//SUPPRIMER

function supprimer() {
    courses.splice(courses.indexOf(window.prompt("Supprimez un article de votre choix")), 1);
}

supprimer();

console.log(courses);


//TOUTSUPPRIMER

function toutSup() {
    courses = [];
}

var confirmation = window.prompt("Voulez-vous tout supprimer ?") 
    if (confirmation == "oui") {
        toutSup();
    }

    console.log(courses);

*/



//CORRIGE

